//Reduce the duplicate characters of a string to a desired minimum.
//Example: string = "aaabbbcc", function reduces dupicates down to one and returns "abc."

function reduceString(str, amount) {

};
